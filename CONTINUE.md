# Continue here

Paste into a new chat: **"Read CLAUDE.md and CONTINUE.md, then continue."**

## State (2026-09-16, checkpoint 4)
- Phase 2 core sim works and is tested (82 tests): `game/sim/` = `simulation.gd` (tick loop, think chain,
  combat, economy, movement, spawners/waves), `pathfinding.gd`, `lanes.gd`, `sim_map.gd`, `build_zone.gd`,
  `sim_entity.gd`, `blackboard.gd`, `unit_db.gd`, `sim_constants.gd`. Data: `game/data/units.json`,
  `cards.json`, `maps/*.json` (generated by `tools/extract_units.py`, `tools/extract_maps.py`).
- Sandbox scene `game/main.tscn` renders capsules; keys 1-4 drop squads, 5-6 place spawners.

## Next step (in order, one at a time, test after each)
1. **Card play + commander**: `Commander` gets a deck (12 `cards.json` entries), charges per card
   (`CardTemplate.dws` tables in `docs/original-architecture.md` section 6: cost formula, charge counts,
   recharge cooldown), tier gating (`reTier` cost = card tier vs commander tier), gold/wood payment,
   `play_card(team, slot, target)` validating: Drop inside `Drop` zone (`SimMap.in_zone`), Spawner on a free
   own build field, Building card inside Drop zone. Legendary limit (`upHasLegendaryUnit`, one alive).
2. **Income upgrade** action (wood cost 1500 + 250 per upgrade, cap 10) and the `Nexus` tech replacement
   (`NexusLevel2/3` at tech events, `TWelaEffectReplaceComponent.KeepTakenDamage`).
3. **Projectiles** (`Scripts/Projectiles/*`: speed, hit on arrival) for ranged units, then the modifier
   Read-chain (buffs/auras), then spells (`Scripts/Spells/*.sps` not yet extracted; extend the extractor).

## Rules that bit us
- Run `--import` before `-s tests/run_tests.gd` when new class_name scripts were added.
- Lambdas connected to sim signals must be disconnected in tests (reference cycle = leak warning).
- Template includes (`UnitTemplate`, `DropTemplate`, `SpawnerTemplate`...) carry defaults such as
  `upSpawner`, `eiSpeed`, `eiAttentionrange`; the extractor merges them. Keep it that way.
- Original quirk kept: lanetowers one-shot tier-1 units (100 dmg, range 15). That is correct.
