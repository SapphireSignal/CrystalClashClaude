# Continue here

Paste into a new chat: **"Read CLAUDE.md and CONTINUE.md, then continue."**

## State (2026-09-16, checkpoint 3)
- Phase 2 core sim works and is tested (61 tests): `game/sim/` = `simulation.gd` (tick loop, think chain,
  combat, economy, movement), `pathfinding.gd`, `lanes.gd`, `sim_map.gd`, `sim_entity.gd`, `blackboard.gd`,
  `unit_db.gd`, `sim_constants.gd`. Data: `game/data/units.json`, `cards.json`, `maps/*.json`
  (generated by `tools/extract_units.py`, `tools/extract_maps.py`).
- Sandbox scene `game/main.tscn` renders capsules; keys 1-4 drop squads.

## Next step (in order, one at a time, test after each)
1. **Build grid + spawners**: `TBuildZone` (8x3, corners blocked, per-team position/front/spawn target from
   `Scripts/Scenarios/PvPRed.dws` / `PvPBlue.dws`), spawner cards occupy a cell, wave spawn every 2 game
   ticks (`Nth(2)`) producing `eiWelaCount` units per cell toward the spawn target. Source:
   `GameServer/BaseConflict.EntityComponents.Server.Welas.Special.pas:120-135`, `Map.pas TBuildZone`.
2. **Card play**: cost + charges from `docs/original-architecture.md` section 6 (`CardTemplate.dws`), drop
   placement inside `Drop` zone (radius 3, `SpreadSpawns`), commander gold/wood/tier checks.
3. **Projectiles** (`Scripts/Projectiles/*`), then **modifier Read-chain** for buffs/auras, then spells.

## Rules that bit us
- Run `--import` before `-s tests/run_tests.gd` when new class_name scripts were added.
- Lambdas connected to sim signals must be disconnected in tests (reference cycle = leak warning).
- Original quirk kept: lanetowers one-shot tier-1 units (100 dmg, range 15). That is correct.
